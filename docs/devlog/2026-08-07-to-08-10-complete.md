# JavaTutor Agent 开发日志

## 2026-08-07 — Phase 1：基础工作流搭建

### 背景
将 JavaTutor Agent 从低代码模式迁移到代码模式，完成基础工作流搭建。

### 完成内容

| 任务 | 说明 |
|------|------|
| Task 1: 项目初始化 | 创建 Coze Agent 模板，对齐平台特性 |
| Task 2: parse_context | 实现上下文解析节点，支持编译错误/变量/步骤解析 |
| Task 3: RouteIntent | 关键词匹配路由（data_query / concept / debug / other / compile_error） |
| Task 4: 专家节点 | 实现 data_query / concept / debug / animate / animate_guide 五个专家 |
| Task 5: graph 构建 | 基于 StateGraph 的流程编排，A/B 测试架构 |

### 里程碑
- 27 个单元测试全部通过
- 初始工作流迁移完成

### 关键文件
- `src/graphs/javatutor/graph.py`
- `src/graphs/javatutor/nodes.py`
- `src/graphs/javatutor/state.py`
- `src/graphs/javatutor/prompts.py`

---

## 2026-08-07 — Bug 修复与稳定性

### 修复清单

1. **Coze 平台消息 content 类型适配**
   - 问题：平台返回的 content 可能是 `list[dict]` 而非 `str`
   - 修复：`parse_context` 中增加类型判断，`list` 转 `str`
   - 文件：`src/graphs/javatutor/nodes.py`

2. **build_final 消息重复输出**
   - 问题：LangGraph 流式输出中 AIMessage 被重复拼接
   - 修复：去重后处理，移除相邻重复消息
   - 文件：`src/graphs/javatutor/nodes.py`

3. **LLMClient.invoke() 参数错误**
   - 问题：`llm_config` 作为整体传入而非展开属性
   - 修复：从 `llm_config` 中提取 `model`、`temperature` 等属性单独传参
   - 文件：`src/graphs/javatutor/nodes.py`

4. **流式回答重复输出**
   - 问题：模型在 token 充足时倾向重复输出
   - 修复：`max_completion_tokens` 降至 2048 + 后处理去重安全网
   - 文件：`src/graphs/javatutor/nodes.py`, `config/agent_llm_config.json`

5. **平台回环重发**
   - 问题：`_run_expert` 返回 `AIMessage` 导致平台误判为需重试
   - 修复：改为返回 `{"answer": answer}` 字典
   - 文件：`src/graphs/javatutor/nodes.py`

---

## 2026-08-07 — Phase 1 完成

### 完成内容
- 所有 Phase 1 任务交付
- 文档更新：`docs/plan/initial-work-plan.md` 更新为 v2.0

---

## 2026-08-08 — Analyze 专家节点

### 完成内容
- 新增 `analyze` 专家节点，提供代码复杂度分析
- 输出结构化 JSON：`{time_complexity, space_complexity, suggestions, ...}`
- 测试覆盖：正常分析、异常 JSON 回退、Prompt 结构验证

### 关键文件
- `src/graphs/javatutor/nodes.py`（analyze_node）
- `src/graphs/javatutor/prompts.py`（SYSTEM_PROMPT_ANALYZE）

---

## 2026-08-08 — Markdown 格式规范化

### 背景
Agent 输出的 Markdown 格式损坏：标题 `###` 后无空格、分隔线 `---` 与文字粘连、代码块挤在一行，导致前端渲染为单个 `<p>` 标签。

### 修复
1. **Prompt 层约束**：四个专家 system prompt 追加硬性格式规范
2. **代码层兜底**：`_normalize_md()` 后处理函数
   - `###` 后无空格 → 补空格
   - 代码块围栏后换行
   - `---` 前后换行
3. **验收**：三次 `/stream_run` 验证，输出正确渲染为 `<h3>`、`<pre>`、`<ul>`、`<p>` 块

### 关键文件
- `src/graphs/javatutor/prompts.py`
- `src/graphs/javatutor/nodes.py`（`_normalize_md`）

---

## 2026-08-09 — Task 4: SVG 动画生成器

### 背景
用户要求为排序、搜索、DP、树、图、链表等算法生成 SVG 动画。

### 完成内容

#### 4.1 基础架构
- `src/learning/animation.py`：`build_animation_svg()` 主入口 + 7 个渲染函数
- 7 个 SVG 模板（Jinja2）：`sort_bars`、`search_bars`、`dp_table`、`tree_traversal`、`graph_path`、`linked_list`
- 算法分类器 `classify_algorithm()`：正则匹配 6 类 + other

#### 4.2 算法标签优先机制
- 新增 `_map_tags_to_category()`：从 analyze 节点返回的 `algorithm_tags` 映射动画类别
- `animate_node` 优先使用标签，无法映射时回退 `classify_algorithm`
- 解决 `UserCode` 类名被错误识别为 `other` 的问题

#### 4.3 自适应数据发现
- `_find_array_var()`：自动检测 steps 中变化最大的数组变量
- `_series_from_steps()`：自适应变量名（arr/nums/list/dp...）
- `_render_dp()`：支持一维 DP 表格 + 二维 DP 矩阵双模式

#### 4.4 排序动画交换 Bug 修复
- 根因：柱子绑定位置下标而非值身份，交换时 from_x 始终指向初始位置
- 修复：值身份追踪，`cur_pos` 维护每个值的实时位置，from_x 取上一轮 to_x
- 柱顶数值 `<text>` 跟随柱子移动

#### 4.5 模板增强
- 所有模板新增：`variable_name`（变量名）、`step_count`（步数）、柱顶/格内数值标注
- 数据不足时友好提示，替代 "暂无XX数据"

### 测试
- 30+ 个动画测试，涵盖 7 类算法、自适应发现、一维 DP、值身份追踪
- 全部通过

### 关键文件
- `src/learning/animation.py`
- `assets/svg_templates/*.svg.j2`

---

## 2026-08-10 — Phase 2: Agent 深化流水线

### 背景
将 JavaTutor 从 "规则路由 + 单次回答" 升级为 "LLM 意图分类 + 上下文压缩 + RAG 检索 + 专家生成 + 评论-修订 + 决策追踪" 的深化流水线。

### 完成内容

| 任务 | 文件 | 说明 |
|------|------|------|
| 1. LLM 意图分类 | `intent.py` | 语义分类取代关键词匹配，5 类意图，低置信度 (<0.6) 回退 other |
| 2. 上下文压缩 | `compaction.py` | 步骤 >200 时滑动窗口 (30 步) 摘要，三模式：none/windowed/truncated |
| 3. RAG 知识检索 | `knowledge.py` | 文本分块 → EmbeddingClient (dim=1024) → pgvector HNSW 检索 |
| 4. State 扩展 | `state.py` | 新增 13 个字段：intent_confidence、retrieved_chunks、context_summary 等 |
| 5. Critic-Revise | `critic.py` | 评论节点评估答案质量，不通过时触发修订节点重写 |
| 6. 节点增强 | `nodes.py` | `_resolve_model()` 统一模型解析、RAG 分块注入、`build_final` 决策追踪 |
| 7. 图重写 | `graph.py` | 新流程：parse→compact→route→[RAG→expert→critic→revise→final] |
| 8. 知识资产 | `assets/knowledge/` | 5 条编译错误 + 5 条标准库条目，种子脚本 `scripts/seed_knowledge.py` |

### 环境修复
1. **循环导入**：提取 `llm.py` 独立模块，打破 intent.py ↔ nodes.py 循环依赖
2. **数据库兜底**：`db.py` 添加 SQLite fallback，`PGDATABASE_URL` 未配置时服务仍可启动
3. **类型安全**：修复 `LLMClient` 返回类型注解、`list[str|dict]` 的 strip 检查

### 测试
- 81/81 单元测试通过（34 原有 + 47 新增）
- test_run 端到端验证通过

---

## 2026-08-10 — 数据库配置与 RAG 修复

### 背景
Coze 平台提供了 `DATABASE_URL` 环境变量，此前 `PGDATABASE_URL` 未配置导致服务启动失败。

### 配置
- 创建 `.env` 文件写入 `PGDATABASE_URL`（Neon PostgreSQL）
- `db.py` 通过 `load_dotenv()` 自动加载

### 修复
1. **EmbeddingClient API 不匹配**：`embed_texts()` 返回扁平 `list[float]`，改为逐条调用 `embed_text()`
2. **pgvector 类型转换**：新增 `_vector_to_str()` 将 `list[float]` 转为 `"[0.1,0.2,...]"` 字符串

### 验证
- 10 条知识分块成功导入
- RAG 检索验证：搜索 "Java array index out of bounds" → 命中 ArrayIndexOutOfBoundsException (score=0.757)
- 81/81 测试通过，test_run 端到端验证通过

---

## 2026-08-10 — 知识库使用说明

### 完成内容
- `scripts/seed_knowledge.py` 升级：支持 `--stats` 参数查看统计，幂等去重（同 source 先删后插）
- 三种资产格式：JSON（结构化条目）、Markdown（自然文本）、纯文本
- 文档记录：`docs/devlog/` 开发日志全量补全

### 使用方式
```bash
# 全量灌库（自动去重）
uv run python scripts/seed_knowledge.py

# 查看统计
uv run python scripts/seed_knowledge.py --stats
```